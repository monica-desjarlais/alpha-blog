<!--ActiveRecord stores validation errors in an array called errors. If you have an Article (or another) model, you would
  access the validation errors in a given instance -->


<% if @article.errors.any? %> <!--if there are errors messages -->
<h2>The following errors prevented the article from being created</h2>
<ul>
<% @article.errors.full_messages.each do |msg| %>  <!--loop through each message-->
<li><%= msg %></li>   <!--display all existent messages in a list-->
<% end %>  <!--end of display msg action-->
</ul>
<% end %>  <!--end of the if condition-->

<!-- full_messages() public
Returns all the full error messages in an array -->

<!-- In article.rb we defined all the constraints for the title and description that will show in the error messages-->



<%= form_for @article do |f| %> <!--create form-->
<p>
<%= f.label :title %> <br>
<%= f.text_field :title %>
</p>
<p>
<%= f.label :description %> <br/>
<%= f.text_area :description %>
</p>
<p>
<%= f.submit %>
</p>

<% end %>
<p>
<%= link_to "Back to articles list", articles_path  %>
</p>
