<h1 align="center">Welcome to <%= @user.username %>'s page</h1>

<div class="row">

<div class="col-md-4 col-md-offset-4 center">

<%= gravatar_for @user, size: 150 %> <!-- a method that creates a gravatar for the @user var, identified from ID, see controller -->

</div>

</div>

<h4 align="center"><%= @user.username %>'s articles</h4> <!-- a listing of all the user's articles -->
<div class="center">
<%= will_paginate @user_articles %> <!--use the instance variable created in show method-users_controller for pagination -->
</div>

<!-- change old article object user.articles with the new paginated object: @user_articles -->
<%= render 'articles/article', obj: @user_articles %> <!-- assign the value of obj:@user_articles -list all the articles associated with the user -->

<div class="center">
<%= will_paginate @user_articles %>
</div>
